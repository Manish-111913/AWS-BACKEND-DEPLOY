const express = require('express');
// Deprecated: abc-test routes removed. Keep empty router to avoid module resolution issues if referenced elsewhere.
const router = express.Router();
module.exports = router;
